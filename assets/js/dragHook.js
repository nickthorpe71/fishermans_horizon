import Sortable from 'sortablejs';


export default {
    mounted() {
        let dragged;
        const hook = this;
        const selector = "#" + this.el.id;

        console.log('The selector is:', selector);
    }
};